server {
  listen       *:80 default;
  server_name  ${APP_DOMAIN};
  
  location /.well-known/acme-challenge {
    proxy_pass http://${DOCKER_CONTROL_HOST};
  }
  
  location / {
    root /usr/share/nginx/html;
  }

}
